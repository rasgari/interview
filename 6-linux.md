حتماً. این پاسخ‌ها برای آزمون شرکت، مصاحبه امنیت، LPIC-1، RHCSA و CEH مناسب هستند. کوتاه، دقیق و قابل حفظ کردن.
________________________________________
Linux Commands
۱. chmod
تعریف:
تغییر سطح دسترسی فایل یا پوشه.
مثال:
chmod 755 file.sh
________________________________________
۲. chown
تعریف:
تغییر مالک (Owner) و گروه (Group) فایل یا پوشه.
مثال:
chown user:user file.txt
________________________________________
۳. grep
تعریف:
جستجوی متن یا الگو داخل فایل.
مثال:
grep "root" /etc/passwd
________________________________________
۴. awk
تعریف:
پردازش متن و استخراج ستون‌های فایل.
مثال:
awk '{print $1}' file.txt
________________________________________
۵. sed
تعریف:
ویرایش و جایگزینی متن در فایل یا خروجی.
مثال:
sed 's/admin/user/g' file.txt
________________________________________
۶. find
تعریف:
جستجوی فایل یا پوشه بر اساس نام، اندازه، زمان و...
مثال:
find / -name "*.log"
________________________________________
۷. locate
تعریف:
جستجوی سریع فایل با استفاده از پایگاه داده ایندکس.
مثال:
locate passwd
________________________________________
۸. tar
تعریف:
فشرده‌سازی یا استخراج فایل‌های آرشیو.
مثال:
tar -cvf backup.tar folder/
________________________________________
۹. gzip
تعریف:
فشرده کردن فایل با پسوند .gz
مثال:
gzip file.txt
________________________________________
۱۰. systemctl
تعریف:
مدیریت سرویس‌ها در سیستم‌های Linux مبتنی بر systemd.
مثال:
systemctl restart nginx
________________________________________
۱۱. journalctl
تعریف:
مشاهده Logهای سیستم و سرویس‌ها.
مثال:
journalctl -xe
________________________________________
۱۲. ps
تعریف:
نمایش پردازش‌های در حال اجرا.
مثال:
ps aux
________________________________________
۱۳. top
تعریف:
نمایش لحظه‌ای مصرف CPU، RAM و پردازش‌ها.
مثال:
top
________________________________________
۱۴. netstat
تعریف:
نمایش اتصالات شبکه، پورت‌ها و Routeها.
مثال:
netstat -tulnp
________________________________________
۱۵. ss
تعریف:
نسخه جدیدتر و سریع‌تر netstat برای مشاهده Socketها و پورت‌ها.
مثال:
ss -tulnp
________________________________________
۱۶. curl
تعریف:
ارسال درخواست HTTP و دریافت پاسخ از سرور.
مثال:
curl https://example.com
________________________________________
۱۷. wget
تعریف:
دانلود فایل از اینترنت.
مثال:
wget https://example.com/file.zip
________________________________________
۱۸. ip
تعریف:
مدیریت تنظیمات شبکه، IP و Route.
مثال:
ip addr
________________________________________
۱۹. ifconfig
تعریف:
نمایش یا تنظیم IP کارت شبکه (ابزار قدیمی، جایگزین آن ip است).
مثال:
ifconfig
________________________________________
۲۰. iptables
تعریف:
فایروال لینوکس برای فیلتر کردن بسته‌های شبکه.
مثال:
iptables -L
________________________________________
۲۱. nftables
تعریف:
نسل جدید فایروال لینوکس و جایگزین iptables.
مثال:
nft list ruleset
________________________________________
جدول حفظی آزمون
دستور	کاربرد
chmod	تغییر سطح دسترسی فایل
chown	تغییر مالک فایل
grep	جستجوی متن
awk	پردازش و استخراج ستون‌ها
sed	ویرایش و جایگزینی متن
find	جستجوی فایل
locate	جستجوی سریع فایل
tar	آرشیو و استخراج فایل
gzip	فشرده‌سازی فایل
systemctl	مدیریت سرویس‌ها
journalctl	مشاهده لاگ سیستم
ps	نمایش پردازش‌ها
top	نمایش مصرف منابع
netstat	نمایش اتصالات شبکه
ss	نمایش Socketها و پورت‌ها
curl	ارسال درخواست HTTP
wget	دانلود فایل
ip	مدیریت IP و شبکه
ifconfig	نمایش تنظیمات شبکه (قدیمی)
iptables	فایروال لینوکس
nftables	فایروال جدید لینوکس
________________________________________
⭐ سوالات پرتکرار آزمون شرکت
تفاوت chmod و chown
•	chmod: تغییر مجوز (Permission)
•	chown: تغییر مالک (Owner)
________________________________________
تفاوت grep و find
•	grep: جستجو داخل محتوای فایل
•	find: جستجوی فایل یا پوشه
________________________________________
تفاوت find و locate
•	find: جستجوی لحظه‌ای روی فایل‌سیستم (دقیق‌تر ولی کندتر)
•	locate: جستجو بر اساس پایگاه داده ایندکس (سریع‌تر، ممکن است به‌روز نباشد)
________________________________________
تفاوت curl و wget
•	curl: ارسال درخواست HTTP/HTTPS و کار با APIها
•	wget: دانلود فایل و پشتیبانی از ادامه دانلود (Resume)
________________________________________
تفاوت netstat و ss
•	netstat: ابزار قدیمی برای مشاهده اتصالات شبکه
•	ss: ابزار جدیدتر، سریع‌تر و جایگزین netstat
________________________________________
تفاوت iptables و nftables
•	iptables: فایروال سنتی لینوکس
•	nftables: نسل جدید فایروال، ساده‌تر، سریع‌تر و جایگزین iptables
________________________________________
تفاوت ps و top
•	ps: نمایش یک‌باره لیست پردازش‌ها
•	top: نمایش زنده و لحظه‌ای مصرف CPU، RAM و پردازش‌ها
________________________________________
تفاوت tar و gzip
•	tar: آرشیو کردن چند فایل یا پوشه (بدون فشرده‌سازی به‌تنهایی)
•	gzip: فشرده‌سازی یک فایل (اغلب همراه tar استفاده می‌شود، مانند tar.gz)
این موارد از پرتکرارترین سؤالات مفهومی لینوکس در آزمون‌های امنیت و مصاحبه‌های فنی هستند و دانستن تفاوت آن‌ها معمولاً امتیاز خوبی در آزمون ایجاد می‌کند.

