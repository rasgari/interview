حتماً. اگر برای آزمون شرکت یا مصاحبه فنی امنیت می‌خواهی، بهترین حالت این است که برای هر مورد تعریف + یک مثال را بلد باشی. این نسخه کوتاه، دقیق و مناسب حفظ کردن است.
________________________________________
OWASP Top 10 (2021)
________________________________________
۱. Broken Access Control
تعریف:
کاربر به منابع یا عملیاتی دسترسی پیدا می‌کند که مجاز به انجام آن نیست.
مثال:
کاربر عادی با تغییر شناسه در URL به اطلاعات کاربر دیگر دسترسی پیدا می‌کند (IDOR).
________________________________________
۲. Cryptographic Failures
تعریف:
استفاده نادرست از رمزنگاری یا عدم محافظت از اطلاعات حساس.
مثال:
ذخیره رمز عبور به‌صورت متن ساده (Plain Text) یا استفاده از الگوریتم‌های ضعیف مانند MD5.
________________________________________
۳. Injection
تعریف:
ورود داده مخرب به برنامه که باعث اجرای دستورات ناخواسته می‌شود.
مثال:
SQL Injection که مهاجم با تزریق دستورات SQL به پایگاه داده دسترسی پیدا می‌کند.
________________________________________
۴. Insecure Design
تعریف:
ضعف امنیتی ناشی از طراحی نامناسب سیستم، نه صرفاً خطای برنامه‌نویسی.
مثال:
نبود محدودیت تعداد دفعات ورود (Rate Limiting) و امکان حمله Brute Force.
________________________________________
۵. Security Misconfiguration
تعریف:
تنظیمات نادرست امنیتی در سرور، نرم‌افزار یا سرویس‌ها.
مثال:
فعال بودن Directory Listing یا استفاده از رمز عبور پیش‌فرض.
________________________________________
۶. Vulnerable and Outdated Components
تعریف:
استفاده از کتابخانه‌ها، فریم‌ورک‌ها یا نرم‌افزارهای دارای آسیب‌پذیری شناخته‌شده.
مثال:
استفاده از نسخه آسیب‌پذیر Log4j که منجر به حمله Log4Shell می‌شود.
________________________________________
۷. Identification and Authentication Failures
تعریف:
ضعف در احراز هویت یا مدیریت نشست کاربران.
مثال:
رمز عبور ضعیف، Session ID قابل حدس یا نبود احراز هویت چندمرحله‌ای (MFA).
________________________________________
۸. Software and Data Integrity Failures
تعریف:
عدم اطمینان از صحت و یکپارچگی نرم‌افزار یا داده‌ها.
مثال:
به‌روزرسانی نرم‌افزار بدون بررسی امضای دیجیتال یا دانلود وابستگی از منبع نامعتبر.
________________________________________
۹. Security Logging and Monitoring Failures
تعریف:
ثبت نکردن یا پایش نکردن رویدادهای امنیتی، که باعث دیر تشخیص دادن حملات می‌شود.
مثال:
عدم ثبت تلاش‌های ناموفق ورود یا نبود هشدار برای فعالیت‌های مشکوک.
________________________________________
۱۰. SSRF (Server-Side Request Forgery)
تعریف:
مهاجم سرور را مجبور می‌کند درخواست‌هایی به منابع داخلی یا خارجی ارسال کند.
مثال:
ارسال درخواست از طریق سرور به سرویس داخلی که مستقیماً از اینترنت قابل دسترسی نیست.
________________________________________
جدول حفظی برای آزمون
مورد	تعریف کوتاه	مثال
Broken Access Control	دسترسی غیرمجاز به منابع	IDOR
Cryptographic Failures	ضعف در رمزنگاری	ذخیره رمز به‌صورت Plain Text
Injection	تزریق کد مخرب	SQL Injection
Insecure Design	طراحی ناامن سیستم	نبود Rate Limiting
Security Misconfiguration	پیکربندی اشتباه	رمز پیش‌فرض، Directory Listing
Vulnerable Components	استفاده از نرم‌افزار آسیب‌پذیر	Log4Shell
Authentication Failures	ضعف در احراز هویت	Session ضعیف، نبود MFA
Software Integrity Failures	عدم بررسی صحت نرم‌افزار	آپدیت بدون امضای دیجیتال
Logging & Monitoring Failures	ثبت و مانیتورینگ ناکافی	ثبت نشدن لاگ ورود ناموفق
SSRF	ارسال درخواست توسط سرور	دسترسی به سرویس‌های داخلی
________________________________________
نکات طلایی برای آزمون شرکت
•	Broken Access Control پرتکرارترین مورد OWASP Top 10 است.
•	تفاوت Authentication (احراز هویت) و Authorization (مجوز دسترسی) را به‌خوبی بدان.
•	برای Injection، SQL Injection رایج‌ترین مثال است.
•	برای Cryptographic Failures، مثال‌هایی مانند MD5، SHA1، ذخیره رمز بدون هش یا بدون Salt را به خاطر داشته باش.
•	برای Security Misconfiguration، نمونه‌های متداول شامل رمز عبور پیش‌فرض، فعال بودن Directory Listing و پیام‌های خطای بیش از حد افشاگر هستند.
•	برای SSRF، به خاطر بسپار که سرور به نمایندگی از مهاجم درخواست ارسال می‌کند، نه مرورگر کاربر.
این سطح از پاسخ معمولاً برای آزمون‌های مفهومی و مصاحبه‌های فنی مناسب است و نشان می‌دهد علاوه بر حفظ تعریف، کاربرد هر مورد را نیز درک کرده‌ای.

