حتماً. برای آزمون شرکت بهتر است پاسخ‌ها تعریفی، کوتاه، دقیق و کاربردی باشند. در ادامه هر سؤال را به‌صورت خلاصه و مناسب مرور آزمون آورده‌ام.
________________________________________
۱. تفاوت Stateful و Stateless Firewall چیست؟
Stateful Firewall:
•	وضعیت ارتباط (Session) را بررسی می‌کند.
•	ترافیک ورودی و خروجی را به صورت هوشمند کنترل می‌کند.
•	امنیت بالاتری دارد.
Stateless Firewall:
•	هر بسته را مستقل بررسی می‌کند.
•	وضعیت ارتباط را ذخیره نمی‌کند.
•	سرعت بیشتر ولی امنیت کمتر دارد.
________________________________________
۲. ACL چیست؟
Access Control List
لیستی از قوانین برای اجازه یا مسدود کردن ترافیک بر اساس IP، پروتکل یا پورت.
________________________________________
۳. DMZ چیست؟
Demilitarized Zone
شبکه‌ای بین اینترنت و شبکه داخلی که سرورهای عمومی مانند Web Server و Mail Server در آن قرار می‌گیرند تا امنیت شبکه داخلی حفظ شود.
________________________________________
۴. NAT چند نوع دارد؟
•	Static NAT
•	Dynamic NAT
•	PAT (Port Address Translation یا NAT Overload)
وظیفه NAT تبدیل IP خصوصی به عمومی و بالعکس است.
________________________________________
۵. VLAN چیست؟
Virtual LAN
تقسیم منطقی یک شبکه فیزیکی به چند شبکه مجزا برای افزایش امنیت و کاهش Broadcast.
________________________________________
۶. STP و RSTP چیست؟
STP (Spanning Tree Protocol):
از ایجاد Loop در شبکه جلوگیری می‌کند.
RSTP (Rapid STP):
نسخه سریع‌تر STP با زمان همگرایی کمتر.
________________________________________
۷. OSPF چگونه کار می‌کند؟
OSPF یک پروتکل مسیریابی Link-State است که با استفاده از الگوریتم Dijkstra کوتاه‌ترین مسیر را محاسبه می‌کند.
________________________________________
۸. BGP چیست؟
Border Gateway Protocol
پروتکل مسیریابی بین Autonomous Systemها در اینترنت که بر اساس سیاست‌های مسیریابی عمل می‌کند.
________________________________________
۹. تفاوت Hub و Switch چیست؟
Hub:
•	در لایه ۱
•	داده را برای همه پورت‌ها ارسال می‌کند.
Switch:
•	در لایه ۲
•	بر اساس MAC Address فقط به مقصد ارسال می‌کند.
________________________________________
۱۰. DHCP چگونه IP تخصیص می‌دهد؟
فرآیند DORA:
•	Discover
•	Offer
•	Request
•	Acknowledge
________________________________________
۱۱. DNS Spoofing چیست؟
حمله‌ای که در آن پاسخ DNS جعل می‌شود و کاربر به سایت جعلی هدایت می‌شود.
________________________________________
۱۲. ARP Poisoning چیست؟
ارسال پیام‌های ARP جعلی برای تغییر جدول ARP قربانی و اجرای حمله Man-in-the-Middle.
________________________________________
۱۳. MAC Flooding چیست؟
ارسال تعداد زیادی MAC جعلی به Switch تا جدول CAM پر شود و سوئیچ مانند Hub رفتار کند.
________________________________________
۱۴. Port Security چیست؟
قابلیتی در Switch که تعداد یا MAC مجاز روی هر پورت را محدود می‌کند تا از اتصال دستگاه‌های غیرمجاز جلوگیری شود.
________________________________________
۱۵. VPN چیست؟
Virtual Private Network
ایجاد یک تونل رمزنگاری‌شده برای ارتباط امن بین کاربران یا شعب از طریق اینترنت.
________________________________________
۱۶. IPSec چگونه کار می‌کند؟
IPSec با استفاده از:
•	AH (Authentication Header)
•	ESP (Encapsulating Security Payload)
داده‌ها را احراز هویت، رمزنگاری و از تغییر آن‌ها جلوگیری می‌کند.
________________________________________
۱۷. تفاوت SSL VPN و IPSec VPN
SSL VPN
•	مبتنی بر TLS/SSL
•	معمولاً از طریق مرورگر
•	مناسب کاربران راه دور
IPSec VPN
•	در لایه شبکه
•	نیاز به Client
•	مناسب ارتباط سایت به سایت و شعب
________________________________________
۱۸. AAA چیست؟
Authentication
احراز هویت
Authorization
تعیین سطح دسترسی
Accounting
ثبت گزارش فعالیت کاربران
________________________________________
۱۹. تفاوت Radius و TACACS+
RADIUS
•	UDP
•	رمزنگاری فقط Password
•	مناسب احراز هویت کاربران شبکه
TACACS+
•	TCP
•	رمزنگاری کل Packet
•	مناسب مدیریت تجهیزات شبکه
•	امنیت بیشتر
________________________________________
۲۰. NAC چیست؟
Network Access Control
سیستمی برای کنترل اینکه چه دستگاه‌هایی اجازه ورود به شبکه را دارند و بررسی وضعیت امنیتی آن‌ها قبل از اتصال.
________________________________________
۲۱. تفاوت IDS و IPS چیست؟
IDS (Intrusion Detection System)
•	فقط حملات را شناسایی و گزارش می‌کند.
•	ترافیک را مسدود نمی‌کند.
IPS (Intrusion Prevention System)
•	حملات را شناسایی و به‌صورت خودکار مسدود می‌کند.
•	به‌صورت Inline در مسیر ترافیک قرار می‌گیرد.
________________________________________
نکات طلایی برای آزمون
سؤالاتی که معمولاً زیاد تکرار می‌شوند:
•	تفاوت Stateful و Stateless Firewall
•	تفاوت IDS و IPS
•	تفاوت Hub و Switch
•	تفاوت RADIUS و TACACS+
•	تفاوت SSL VPN و IPSec VPN
•	مراحل DHCP (DORA)
•	انواع NAT
•	تفاوت STP و RSTP
•	مفهوم AAA
•	VLAN و DMZ
این مفاهیم جزو مباحث پایه و پرتکرار در آزمون‌های امنیت شبکه و معمولاً در مصاحبه‌های فنی نیز مطرح می‌شوند.

