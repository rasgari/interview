حتماً. این بخش یکی از مهم‌ترین مباحث آزمون شرکت، OWASP API Security Top 10، CEH، eJPT و مصاحبه‌های تست نفوذ وب است. پاسخ‌ها را به صورت کوتاه، مفهومی و قابل حفظ کردن آماده کرده‌ام.
________________________________________
API Security
۱. REST (Representational State Transfer)
تعریف:
سبک معماری برای طراحی API که از پروتکل HTTP استفاده می‌کند.
ویژگی‌ها:
•	Stateless
•	استفاده از متدهای HTTP (GET، POST، PUT، DELETE و...)
•	معمولاً داده‌ها با JSON تبادل می‌شوند.
________________________________________
۲. SOAP (Simple Object Access Protocol)
تعریف:
پروتکل استاندارد برای تبادل اطلاعات بین سرویس‌ها.
ویژگی‌ها:
•	مبتنی بر XML
•	امنیت و استانداردهای بیشتر
•	پیچیده‌تر از REST
________________________________________
۳. JWT (JSON Web Token)
تعریف:
توکن استاندارد برای احراز هویت و انتقال اطلاعات بین Client و Server.
ساختار:
•	Header
•	Payload
•	Signature
کاربرد:
Authentication و Authorization
________________________________________
۴. OAuth2
تعریف:
چارچوب (Framework) برای اعطای مجوز دسترسی به منابع بدون اشتراک‌گذاری رمز عبور.
مثال:
ورود به یک سایت با حساب Google یا GitHub.
________________________________________
۵. OpenID Connect (OIDC)
تعریف:
لایه‌ای روی OAuth2 برای احراز هویت کاربران.
کاربرد:
تأیید هویت (Authentication)
نکته:
•	OAuth2 = Authorization
•	OpenID Connect = Authentication
________________________________________
۶. Rate Limit
تعریف:
محدود کردن تعداد درخواست‌های یک کاربر یا IP در بازه زمانی مشخص.
هدف:
•	جلوگیری از Brute Force
•	جلوگیری از DoS
•	جلوگیری از Abuse
________________________________________
۷. Broken Object Level Authorization (BOLA)
تعریف:
آسیب‌پذیری‌ای که در آن کاربر می‌تواند به منابع متعلق به کاربران دیگر دسترسی پیدا کند، چون کنترل دسترسی در سطح شیء به‌درستی اعمال نشده است.
مثال:
/api/users/100
کاربر شناسه را به:
/api/users/101
تغییر می‌دهد و اطلاعات کاربر دیگری را مشاهده می‌کند.
________________________________________
۸. API Gateway
تعریف:
دروازه ورود درخواست‌ها به سرویس‌های API.
وظایف:
•	Authentication
•	Authorization
•	Rate Limiting
•	Logging
•	Load Balancing
•	Routing
________________________________________
جدول حفظی آزمون
مورد	تعریف کوتاه
REST	API مبتنی بر HTTP
SOAP	پروتکل API مبتنی بر XML
JWT	توکن احراز هویت
OAuth2	اعطای مجوز (Authorization)
OpenID Connect	احراز هویت (Authentication)
Rate Limit	محدودسازی تعداد درخواست
BOLA	دسترسی غیرمجاز به آبجکت‌ها
API Gateway	مدیریت و کنترل درخواست‌های API
________________________________________
⭐ سوالات پرتکرار آزمون شرکت
REST چیست؟
سبک معماری API مبتنی بر HTTP و معمولاً با فرمت JSON.
________________________________________
SOAP چیست؟
پروتکل تبادل اطلاعات مبتنی بر XML با استانداردهای امنیتی بیشتر.
________________________________________
تفاوت REST و SOAP
REST	SOAP
سبک معماری	پروتکل
JSON	XML
سریع‌تر	سنگین‌تر
ساده	پیچیده‌تر
________________________________________
JWT چیست؟
توکنی شامل Header، Payload و Signature که برای احراز هویت و انتقال اطلاعات استفاده می‌شود.
________________________________________
OAuth2 چیست؟
چارچوب اعطای مجوز دسترسی به منابع بدون اشتراک‌گذاری رمز عبور.
________________________________________
OpenID Connect چیست؟
پروتکل احراز هویت مبتنی بر OAuth2.
________________________________________
تفاوت OAuth2 و OpenID Connect
OAuth2
•	Authorization
•	مدیریت دسترسی
OpenID Connect
•	Authentication
•	تأیید هویت کاربر
________________________________________
Rate Limit چیست؟
محدود کردن تعداد درخواست‌های یک کاربر یا IP برای جلوگیری از سوءاستفاده.
________________________________________
BOLA چیست؟
آسیب‌پذیری ناشی از نبود کنترل دسترسی مناسب در سطح شیء که باعث دسترسی به داده‌های سایر کاربران می‌شود.
________________________________________
API Gateway چیست؟
دروازه مدیریت درخواست‌های API که وظایفی مانند احراز هویت، کنترل دسترسی، ثبت لاگ، محدودسازی نرخ درخواست و مسیریابی را انجام می‌دهد.
________________________________________
⭐ ساختار JWT
Header
     │
Payload
     │
Signature
________________________________________
⭐ تفاوت Authentication و Authorization
Authentication	Authorization
احراز هویت	تعیین سطح دسترسی
Who are you?	What can you access?
________________________________________
⭐ ابزارها و مفاهیم مرتبط
مفهوم	کاربرد
REST	طراحی API
SOAP	سرویس‌های سازمانی
JWT	توکن احراز هویت
OAuth2	مجوز دسترسی
OpenID Connect	احراز هویت
Rate Limit	جلوگیری از Abuse
BOLA	کنترل دسترسی سطح شیء
API Gateway	مدیریت API
________________________________________
🎯 نکات طلایی برای آزمون
•	REST = معماری API مبتنی بر HTTP و معمولاً با JSON.
•	SOAP = پروتکل API مبتنی بر XML، مناسب محیط‌های سازمانی با نیاز به استانداردهای سخت‌گیرانه.
•	JWT = توکن سه‌بخشی شامل Header، Payload و Signature.
•	OAuth2 = برای Authorization (اعطای مجوز).
•	OpenID Connect = برای Authentication (احراز هویت) و بر پایه OAuth2.
•	Rate Limit = جلوگیری از حملات Brute Force، سوءاستفاده از API و DoS.
•	BOLA = یکی از مهم‌ترین آسیب‌پذیری‌های OWASP API Top 10؛ ناشی از نبود کنترل دسترسی مناسب روی شناسه اشیاء.
•	API Gateway = نقطه ورود APIها که مسئول احراز هویت، کنترل دسترسی، محدودسازی نرخ درخواست، ثبت لاگ و مسیریابی است.
________________________________________
🔥 سوال بسیار پرتکرار آزمون
تفاوت OAuth2 و OpenID Connect چیست؟
•	OAuth2: برای Authorization (اعطای مجوز دسترسی به منابع).
•	OpenID Connect (OIDC): برای Authentication (احراز هویت کاربر) و بر پایه OAuth2 ساخته شده است.
این تفاوت از رایج‌ترین سؤالات آزمون‌های شرکت و مصاحبه‌های امنیت API است.

